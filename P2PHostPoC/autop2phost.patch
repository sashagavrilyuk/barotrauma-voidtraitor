diff --git a/Barotrauma/BarotraumaClient/ClientSource/GameMain.cs b/Barotrauma/BarotraumaClient/ClientSource/GameMain.cs
--- a/Barotrauma/BarotraumaClient/ClientSource/GameMain.cs
+++ b/Barotrauma/BarotraumaClient/ClientSource/GameMain.cs
@@ -122,6 +122,7 @@ namespace Barotrauma
 
         public Option<ConnectCommand> ConnectCommand = Option<ConnectCommand>.None();
         private string clientName;
+        private bool autoP2PHostStarted;
 
         private static SpriteBatch spriteBatch;
 
@@ -837,6 +838,12 @@ namespace Barotrauma
                 }
                 else if (HasLoaded)
                 {
+                    if (!autoP2PHostStarted && ConsoleArguments.Contains("-autop2phost"))
+                    {
+                        autoP2PHostStarted = true;
+                        MainMenuScreen.StartP2PServerFromCommandLine();
+                    }
+
                     if (ConnectCommand.TryUnwrap(out var connectCommand))
                     {
                         DebugConsole.NewMessage($"Processing connect command: {connectCommand}...", Color.Lime);
diff --git a/Barotrauma/BarotraumaClient/ClientSource/Screens/MainMenuScreen/MainMenuScreen.cs b/Barotrauma/BarotraumaClient/ClientSource/Screens/MainMenuScreen/MainMenuScreen.cs
--- a/Barotrauma/BarotraumaClient/ClientSource/Screens/MainMenuScreen/MainMenuScreen.cs
+++ b/Barotrauma/BarotraumaClient/ClientSource/Screens/MainMenuScreen/MainMenuScreen.cs
@@ -1022,6 +1022,23 @@ namespace Barotrauma
             return true;
         }
 
+        public void StartP2PServerFromCommandLine()
+        {
+            if (!CanHostServer())
+            {
+                DebugConsole.ThrowError("Cannot start P2P server: Steam/EOS networking is not initialized.");
+                return;
+            }
+            if (string.IsNullOrWhiteSpace(serverNameBox.Text))
+            {
+                DebugConsole.ThrowError($"Cannot start P2P server: server name is empty in \"{ServerSettings.SettingsFile}\".");
+                return;
+            }
+
+            DebugConsole.NewMessage("Starting server automatically using P2P networking...", Color.Lime);
+            TryStartServer();
+        }
+
         private void TryStartServer()
         {
             if (SubmarineInfo.SavedSubmarines.Any(s => s.CalculatingHash))
