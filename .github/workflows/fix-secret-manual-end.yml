name: Fix Secret manual ending

on:
  push:
    branches: [Ai-temp]
    paths:
      - .github/workflows/fix-secret-manual-end.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: Ai-temp
          fetch-depth: 0
      - run: python3 .github/fix_secret_manual_end.py
      - run: git diff --check
      - run: |
          git config user.name "kotofey51"
          git config user.email "38777263+sashagavrilyuk@users.noreply.github.com"
          git add traitormod-voidtraitor-dev-ai/Lua/config/pointshop/traitors/cultist.lua traitormod-voidtraitor-dev-ai/Lua/gamemodes/secret.lua traitormod-voidtraitor-dev-ai/Lua/commands.lua traitormod-voidtraitor-dev-ai/Lua/clientmenu.lua traitormod-voidtraitor-dev-ai/Lua/language/clientmenu_admin.lua traitormod-voidtraitor-dev-ai/Lua/language/english.lua traitormod-voidtraitor-dev-ai/Lua/language/russian.lua
          git commit -m "Fix Secret manual ending and add instant admin end"
          git push origin HEAD:Ai-temp
