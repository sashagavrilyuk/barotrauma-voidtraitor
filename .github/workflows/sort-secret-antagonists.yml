name: Sort Secret antagonists first

on:
  push:
    branches: [Ai-temp]
    paths:
      - .github/workflows/sort-secret-antagonists.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: Ai-temp
          fetch-depth: 0
      - run: python3 .github/sort_secret_antagonists.py
      - run: git diff --check
      - run: |
          git config user.name "kotofey51"
          git config user.email "38777263+sashagavrilyuk@users.noreply.github.com"
          git add traitormod-voidtraitor-dev-ai/Lua/gamemodes/secret.lua
          git commit -m "Sort Secret antagonists first in round summary"
          git push origin HEAD:Ai-temp
